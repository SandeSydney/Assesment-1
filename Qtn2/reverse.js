const manipulator = (userStr)=>{
    let origString = userStr
    let modString = origString.slice(0,4)
    let newMod = modString.reverse()
    console.log(modString.reverse);
}

manipulator("Lorem at")